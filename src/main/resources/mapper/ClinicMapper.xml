<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hohoedu.book_clinic.clinic.ClinicRepository">

    <select id="findCenterCode" resultType="java.lang.String">
        SELECT TOP 1 center_code
        FROM erp_student
        WHERE student_id = #{studentId}
    </select>

    <select id="findGradeKey" resultType="java.lang.String">
        SELECT TOP 1 grade_key
        FROM erp_student
        WHERE student_id = #{studentId}
    </select>

    <!-- 클리닉 추천 기준 학년(book_clinic 자체 관리) — 올패스 grade_key와 별개, 없으면 null -->
    <select id="findClinicGradeKey" resultType="java.lang.String">
        SELECT TOP 1 clinic_grade_key
        FROM erp_student
        WHERE student_id = #{studentId}
    </select>

    <update id="updateClinicGradeKey">
        UPDATE erp_student
        SET clinic_grade_key = #{schoolyear}
        WHERE student_id = #{studentId}
    </update>

    <select id="findLastRecommend" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$LastRecommendDTO">
        SELECT TOP 1
               bc.content_type AS contentType,
               bc.genre        AS genre
        FROM erp_bookstore_recommend_log rl
        JOIN erp_bookstore_content bc ON bc.content_id = rl.content_id
        WHERE rl.student_id = #{studentId}
        ORDER BY rl.recommend_id DESC
    </select>

    <!-- 심화 게이트(2026-08-31) — 가장 최근 완독(DONE)한 추천의 도서 + 완독일(KST 날짜). completed_at은
         첫 제출 시 채워진다(updateRecommendResult). 완독 이력이 없으면 결과 없음(null). -->
    <select id="findLastDoneForGate" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$AdvancedGateDTO">
        SELECT TOP 1
               content_id                AS contentId,
               CAST(completed_at AS DATE) AS completedDate
        FROM erp_bookstore_recommend_log
        WHERE student_id = #{studentId} AND status = 'DONE'
        ORDER BY recommend_id DESC
    </select>

    <!--
      우선순위(sort_order) 순으로 스캔하며 아래 조건을 모두 만족하는 첫 "item(실물 판본)"을 단일
      쿼리로 선택한다(2026-07-30 재설계 / 2026-08-28 제외 기준을 item → content로 복원):
        1) 이 학생에게 이 책(content)이 아직 추천된 적 없을 것 (recommend_log.content_id 기준).
           예전엔 item_id 기준이라 같은 책이 사본(item) 여러 개로 등록돼 있으면 다 읽은 책이
           다른 판본으로 다시 추천됐다(문제는 content 기준이라 같은 문제를 다시 푸는 꼴).
        2) 그 item에 대여 가능한 실물 재고(qty - loaned_qty - lost_qty > 0)가 있을 것 — 재고만큼
           여러 학생에게 동시에 같은 item이 추천될 수 있다
        3) applyDedup=true일 때만: 직전 추천 도서와 분류·장르가 모두 같으면 제외(분류/장르는 content 속성)
      우선순위(p.sort_order)는 content 기준이라, 같은 content에 재고 있는 item이 여러 개면 item_id
      오름차순으로 하나를 정한다(어느 판본이든 문제는 동일하므로 순서 자체는 중요하지 않음).
    -->
    <select id="pickNextItem" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$PickedItemDTO">
        SELECT TOP 1 i.item_id AS itemId, c.content_id AS contentId
        FROM erp_bookstore_priority p
        JOIN erp_bookstore_priority_draft d
            ON d.draft_id = p.draft_id AND d.is_active = 'Y'
           AND d.year = #{year} AND d.schoolyear = #{schoolyear}
        JOIN erp_bookstore_content c
            ON c.content_id = p.content_id AND c.state = 'Y'
        JOIN erp_bookstore_item i
            ON i.content_id = c.content_id AND i.center_code = #{centerCode}
           AND i.qty - i.loaned_qty - i.lost_qty > 0
        WHERE NOT EXISTS (
            SELECT 1 FROM erp_bookstore_recommend_log rl
            WHERE rl.student_id = #{studentId} AND rl.content_id = c.content_id
        )
        <if test="applyDedup">
        AND NOT (
            #{lastType} IS NOT NULL AND #{lastGenre} IS NOT NULL
            AND c.content_type = #{lastType} AND c.genre = #{lastGenre}
        )
        </if>
        <!-- 고른 뒤 원자적 재고 확보(reserveItemById)에 실패한 item은 이번 추천에서 건너뛴다
             (그 사이 다른 학생이 마지막 한 권을 채간 경우 — 에러 대신 다음 후보로 넘어가려고, 2026-08-28) -->
        <if test="excludeItemIds != null and excludeItemIds.size() > 0">
        AND i.item_id NOT IN
        <foreach collection="excludeItemIds" item="ex" open="(" close=")" separator=",">#{ex}</foreach>
        </if>
        ORDER BY p.sort_order ASC, i.item_id ASC
    </select>

    <!--
      학생이 들고 찾아오는 건 센터에 꽂혀 있는 "실물 책"이라, 제목/저자/출판사/표지는 그 센터가 등록한
      item 값을 우선 쓴다(item에 값이 없거나 item이 지워졌으면 마스터 content로 폴백, 2026-07-31).
      줄거리·장르·수상·교육과정·키워드는 item에 없는 정보라 항상 content 기준이다.
    -->
    <select id="findBookCard" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$RecommendBookDTO">
        SELECT bc.content_id     AS contentId,
               it.item_id        AS itemId,
               COALESCE(NULLIF(it.book_title, ''), bc.original_title) AS originalTitle,
               COALESCE(NULLIF(it.author, ''),     bc.author)         AS author,
               COALESCE(NULLIF(it.publisher, ''),  bc.publisher)      AS publisher,
               COALESCE(NULLIF(it.image_url, ''),  bc.image_url)      AS imageUrl,
               bc.summary        AS summary,
               bc.keywords       AS keywords,
               bc.content_type   AS contentType,
               ct.codeNm         AS contentTypeName,
               bc.genre          AS genre,
               g.codeNm          AS genreName,
               dc.name           AS curriculumName,
               dr.name           AS recommendOrgName,
               da.name           AS awardName
        FROM erp_bookstore_content bc
        LEFT JOIN erp_bookstore_item it ON it.item_id = #{itemId}
        LEFT JOIN erp_bookstore_code ct ON ct.gubun = 'C' AND ct.code = bc.content_type
        LEFT JOIN erp_bookstore_code g  ON g.gubun  = 'G' AND g.code  = bc.genre
        LEFT JOIN erp_bookstore_content_detail dc ON dc.content_id = bc.content_id AND dc.gubun = 'C'
        LEFT JOIN erp_bookstore_content_detail dr ON dr.content_id = bc.content_id AND dr.gubun = 'R'
        LEFT JOIN erp_bookstore_content_detail da ON da.content_id = bc.content_id AND da.gubun = 'A'
        WHERE bc.content_id = #{contentId}
    </select>

    <!-- status='PENDING'인 추천만 조회 — DONE 처리된 책은 다시 보여주지 않고 새 추천으로 넘어간다 -->
    <select id="findPendingRecommendBookCard" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$RecommendBookDTO">
        SELECT TOP 1
               rl.item_id        AS itemId,
               bc.content_id     AS contentId,
               COALESCE(NULLIF(it.book_title, ''), bc.original_title) AS originalTitle,
               COALESCE(NULLIF(it.author, ''),     bc.author)         AS author,
               COALESCE(NULLIF(it.publisher, ''),  bc.publisher)      AS publisher,
               COALESCE(NULLIF(it.image_url, ''),  bc.image_url)      AS imageUrl,
               bc.summary        AS summary,
               bc.keywords       AS keywords,
               bc.content_type   AS contentType,
               ct.codeNm         AS contentTypeName,
               bc.genre          AS genre,
               g.codeNm          AS genreName,
               dc.name           AS curriculumName,
               dr.name           AS recommendOrgName,
               da.name           AS awardName
        FROM erp_bookstore_recommend_log rl
        JOIN erp_bookstore_content bc ON bc.content_id = rl.content_id
        LEFT JOIN erp_bookstore_item it ON it.item_id = rl.item_id
        LEFT JOIN erp_bookstore_code ct ON ct.gubun = 'C' AND ct.code = bc.content_type
        LEFT JOIN erp_bookstore_code g  ON g.gubun  = 'G' AND g.code  = bc.genre
        LEFT JOIN erp_bookstore_content_detail dc ON dc.content_id = bc.content_id AND dc.gubun = 'C'
        LEFT JOIN erp_bookstore_content_detail dr ON dr.content_id = bc.content_id AND dr.gubun = 'R'
        LEFT JOIN erp_bookstore_content_detail da ON da.content_id = bc.content_id AND da.gubun = 'A'
        WHERE rl.student_id = #{studentId} AND rl.status = 'PENDING'
        ORDER BY rl.recommend_id DESC
    </select>

    <!-- 홈 화면 "책 추천받기" 대기 상태에 표시할, 가장 최근에 끝낸(DONE) 책 -->
    <select id="findLastDoneBookCard" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$RecommendBookDTO">
        SELECT TOP 1
               rl.item_id        AS itemId,
               bc.content_id     AS contentId,
               COALESCE(NULLIF(it.book_title, ''), bc.original_title) AS originalTitle,
               COALESCE(NULLIF(it.author, ''),     bc.author)         AS author,
               COALESCE(NULLIF(it.publisher, ''),  bc.publisher)      AS publisher,
               COALESCE(NULLIF(it.image_url, ''),  bc.image_url)      AS imageUrl,
               bc.summary        AS summary,
               bc.keywords       AS keywords,
               bc.content_type   AS contentType,
               ct.codeNm         AS contentTypeName,
               bc.genre          AS genre,
               g.codeNm          AS genreName,
               dc.name           AS curriculumName,
               dr.name           AS recommendOrgName,
               da.name           AS awardName
        FROM erp_bookstore_recommend_log rl
        JOIN erp_bookstore_content bc ON bc.content_id = rl.content_id
        LEFT JOIN erp_bookstore_item it ON it.item_id = rl.item_id
        LEFT JOIN erp_bookstore_code ct ON ct.gubun = 'C' AND ct.code = bc.content_type
        LEFT JOIN erp_bookstore_code g  ON g.gubun  = 'G' AND g.code  = bc.genre
        LEFT JOIN erp_bookstore_content_detail dc ON dc.content_id = bc.content_id AND dc.gubun = 'C'
        LEFT JOIN erp_bookstore_content_detail dr ON dr.content_id = bc.content_id AND dr.gubun = 'R'
        LEFT JOIN erp_bookstore_content_detail da ON da.content_id = bc.content_id AND da.gubun = 'A'
        WHERE rl.student_id = #{studentId} AND rl.status = 'DONE'
        ORDER BY rl.recommend_id DESC
    </select>

    <insert id="insertRecommendLog">
        INSERT INTO erp_bookstore_recommend_log (student_id, content_id, item_id)
        VALUES (#{studentId}, #{contentId}, #{itemId})
    </insert>

    <!-- 하루 추천 한도(2권) 판정용 — 그날 이 학생에게 새로 생성된 recommend_log 건수 -->
    <select id="countTodayRecommends" resultType="int">
        SELECT COUNT(*)
        FROM erp_bookstore_recommend_log
        WHERE student_id = #{studentId}
          AND CAST(recommended_at AS DATE) = #{date}
    </select>

    <select id="findRecommendLogStatus" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$RecommendLogStatusDTO">
        SELECT TOP 1
               recommend_id        AS recommendId,
               status              AS status,
               grade               AS grade,
               correct_count       AS correctCount,
               final_correct_count AS finalCorrectCount,
               total_count         AS totalCount
        FROM erp_bookstore_recommend_log
        WHERE student_id = #{studentId} AND content_id = #{contentId}
        ORDER BY recommend_id DESC
    </select>

    <!-- 제출 1회분을 다중 VALUES 한 문장으로 적재 — 같은 문장이라 submitted_at이 전부 동일해 회차 구분에 쓰인다 -->
    <insert id="insertQuizAnswerLogs">
        INSERT INTO erp_bookstore_quiz_answer_log
            (recommend_id, student_id, content_id, qlevel, qnum, selected, is_correct, qtype)
        VALUES
        <foreach collection="answerLogs" item="a" separator=",">
            (#{recommendId}, #{studentId}, #{contentId}, #{qlevel}, #{a.qnum}, #{a.selected}, #{a.correct}, #{a.qtype})
        </foreach>
    </insert>

    <!-- 첫 제출 — 처음 점수/최종 점수를 같은 값으로 세우고, 합격/불합격 무관하게 status=DONE (2026-08-28) -->
    <update id="updateRecommendResult">
        UPDATE erp_bookstore_recommend_log
        SET correct_count       = #{correctCount},
            final_correct_count = #{correctCount},
            total_count         = #{totalCount},
            grade               = #{grade},
            status              = #{status}
            <if test="status == 'DONE'">, completed_at = DATEADD(HOUR, 9, GETUTCDATE())</if>
        WHERE recommend_id = #{recommendId}
    </update>

    <!-- 재도전(mode=RETRY) — 최종 점수·등급을 갱신(둘 다 "올라가기만"; max/higherGrade는 호출부 계산).
         처음 점수·status는 첫 시도 결과로 고정 (2026-08-28) -->
    <update id="updateRetryResult">
        UPDATE erp_bookstore_recommend_log
        SET final_correct_count = #{finalCorrectCount},
            grade               = #{grade}
        WHERE recommend_id = #{recommendId}
    </update>

    <!-- 재도전으로 등급이 올라 상위 뱃지로 교체할 때 — 그 책의 기본 문제 뱃지(1~3)를 먼저 지운다 -->
    <delete id="deleteBasicBadge">
        DELETE FROM erp_bookstore_student_badge
        WHERE student_id = #{studentId} AND content_id = #{contentId} AND badge_id IN (1, 2, 3)
    </delete>

    <select id="findAdvancedBadgeId" resultType="java.lang.Integer">
        SELECT MAX(badge_id)
        FROM erp_bookstore_student_badge
        WHERE student_id = #{studentId} AND content_id = #{contentId} AND badge_id IN (4, 5)
    </select>

    <delete id="deleteAdvancedBadge">
        DELETE FROM erp_bookstore_student_badge
        WHERE student_id = #{studentId} AND content_id = #{contentId} AND badge_id IN (4, 5)
    </delete>

    <!-- 단계 = 학생 학년. 그 학년 도서(content.schoolyear)의 완독(DONE) 권수를 센다 -->
    <select id="countDoneBooksByGrade" resultType="int">
        SELECT COUNT(*)
        FROM erp_bookstore_recommend_log rl
        JOIN erp_bookstore_content c ON c.content_id = rl.content_id
        WHERE rl.student_id = #{studentId}
          AND rl.status = 'DONE'
          AND c.schoolyear = #{schoolyear}
    </select>

    <select id="countDoneBooksThisYear" resultType="int">
        SELECT COUNT(*)
        FROM erp_bookstore_recommend_log
        WHERE student_id = #{studentId}
          AND status = 'DONE'
          AND YEAR(completed_at) = YEAR(DATEADD(HOUR, 9, GETUTCDATE()))
    </select>

    <select id="findCardByContent" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$CardDTO">
        SELECT content_id     AS contentId,
               original_title AS cardName,
               author         AS bookTitle,
               image_url      AS imageUrl
        FROM erp_bookstore_content
        WHERE content_id = #{contentId}
    </select>

    <!-- ══════════════ 카드 지급 이력 (2026-07-28) ══════════════ -->

    <select id="existsNormalCard" resultType="boolean">
        SELECT CASE WHEN EXISTS (
            SELECT 1 FROM erp_bookstore_student_card
            WHERE student_id = #{studentId} AND content_id = #{contentId} AND card_type = 'NORMAL'
        ) THEN 1 ELSE 0 END
    </select>

    <insert id="insertNormalCard">
        INSERT INTO erp_bookstore_student_card (student_id, content_id, card_type)
        VALUES (#{studentId}, #{contentId}, 'NORMAL')
    </insert>

    <select id="countNormalCards" resultType="int">
        SELECT COUNT(*)
        FROM erp_bookstore_student_card
        WHERE student_id = #{studentId} AND card_type = 'NORMAL'
    </select>

    <select id="existsRareCard" resultType="boolean">
        SELECT CASE WHEN EXISTS (
            SELECT 1 FROM erp_bookstore_student_card
            WHERE student_id = #{studentId} AND card_type = 'RARE' AND trigger_count = #{triggerCount}
        ) THEN 1 ELSE 0 END
    </select>

    <insert id="insertRareCard">
        INSERT INTO erp_bookstore_student_card (student_id, card_type, trigger_count)
        VALUES (#{studentId}, 'RARE', #{triggerCount})
    </insert>

    <!-- 보유 카드 전체 — NORMAL은 그 책 표지/제목, RARE는 content_id가 없어 고정 이름/이미지. 최신 획득순 -->
    <select id="findEarnedCards" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$CardDTO">
        SELECT sc.content_id                         AS contentId,
               sc.card_type                          AS cardType,
               CASE WHEN sc.card_type = 'RARE' THEN N'레어 카드' ELSE c.original_title END AS cardName,
               CASE WHEN sc.card_type = 'RARE' THEN NULL ELSE c.author END AS bookTitle,
               CASE WHEN sc.card_type = 'RARE' THEN '/images/student_result/rare.png' ELSE c.image_url END AS imageUrl
        FROM erp_bookstore_student_card sc
        LEFT JOIN erp_bookstore_content c ON c.content_id = sc.content_id
        WHERE sc.student_id = #{studentId}
        ORDER BY sc.earned_at DESC
    </select>

    <select id="findCompletedThisMonth" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$MonthBookDTO">
        SELECT TOP (#{limit})
               c.content_id     AS contentId,
               c.original_title AS originalTitle,
               c.image_url      AS imageUrl,
               'DONE'           AS status
        FROM erp_bookstore_recommend_log rl
        JOIN erp_bookstore_content c ON c.content_id = rl.content_id
        WHERE rl.student_id = #{studentId}
          AND rl.status = 'DONE'
          AND YEAR(rl.completed_at) = YEAR(DATEADD(HOUR, 9, GETUTCDATE()))
          AND MONTH(rl.completed_at) = MONTH(DATEADD(HOUR, 9, GETUTCDATE()))
        ORDER BY rl.completed_at DESC
    </select>

    <select id="findLevelTitle" resultType="java.lang.String">
        SELECT title
        FROM erp_bookstore_level
        WHERE schoolyear = #{schoolyear} AND level_no = #{levelNo}
    </select>

    <!-- ══════════════ 뱃지 판정 (2026-07-10) ══════════════ -->

    <select id="findBadge" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$BadgeDTO">
        SELECT badge_id   AS badgeId,
               badge_name AS badgeName,
               badge_desc AS badgeDesc,
               category   AS category,
               threshold  AS threshold,
               param      AS param
        FROM erp_bookstore_badge
        WHERE badge_id = #{badgeId}
    </select>

    <!-- 학생이 획득한 뱃지 상세 (책마다 부여 — 같은 종류가 여러 책에서 나올 수 있음), 최신 획득순 -->
    <select id="findEarnedBadges" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$BadgeDTO">
        SELECT b.badge_id   AS badgeId,
               b.badge_name AS badgeName,
               b.badge_desc AS badgeDesc,
               b.category   AS category,
               b.threshold  AS threshold,
               b.param      AS param
        FROM erp_bookstore_student_badge sb
        JOIN erp_bookstore_badge b ON b.badge_id = sb.badge_id
        WHERE sb.student_id = #{studentId}
        ORDER BY sb.earned_at DESC
    </select>

    <insert id="insertStudentBadge">
        INSERT INTO erp_bookstore_student_badge (student_id, content_id, badge_id)
        VALUES (#{studentId}, #{contentId}, #{badgeId})
    </insert>

    <!-- 특정 책+난이도(qlevel)에 대한 기존 제출 회차 수 — 0이면 이번이 '첫 시도'(뱃지 등급 판정 기준) -->
    <select id="countPriorAttempts" resultType="int">
        SELECT COUNT(*)
        FROM erp_bookstore_quiz_answer_log
        WHERE student_id = #{studentId} AND content_id = #{contentId} AND qlevel = #{qlevel}
    </select>

    <!-- submitted_at이 같으면 같은 제출 회차라서(ddl-core.sql 주석 참고) DISTINCT로 회차 수를 센다 -->
    <select id="countPriorAttemptRounds" resultType="int">
        SELECT COUNT(DISTINCT submitted_at)
        FROM erp_bookstore_quiz_answer_log
        WHERE recommend_id = #{recommendId} AND qlevel = #{qlevel}
    </select>

    <!-- 문항(qnum)별 가장 최근 제출의 정답 여부 — answer_id가 클수록 최신 제출이다 -->
    <select id="findLatestAnswersByRecommend" resultType="com.hohoedu.book_clinic.clinic._dto.ClinicRespDTO$LatestAnswerDTO">
        SELECT q1.qnum AS qnum, q1.is_correct AS correct
        FROM erp_bookstore_quiz_answer_log q1
        WHERE q1.recommend_id = #{recommendId} AND q1.qlevel = #{qlevel}
          AND q1.answer_id = (
              SELECT MAX(q2.answer_id)
              FROM erp_bookstore_quiz_answer_log q2
              WHERE q2.recommend_id = q1.recommend_id AND q2.qlevel = q1.qlevel AND q2.qnum = q1.qnum
          )
    </select>

</mapper>
